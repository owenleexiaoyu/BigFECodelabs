import React from "react";
import { View, Text } from "react-native";

export default () => {
    return (
        <View className="w-full h-full flex-col justify-center items-center">
            <Text className="text-3xl">我</Text>
        </View>
    );
}